---
description:
🎨 REGLAS PARA IA FRONTEND (Builder Exportado a React)
NO REEMPLAZAR COMPONENTES CREADOS CON BUILDER:

Los componentes existentes (vistas, formularios, botones, tabs, tablas) no deben eliminarse ni ser reemplazados por nuevos componentes genéricos o diferentes.

Solo puedes agregar lógica, props o funciones que permitan adaptarlos al backend real.

No alterar la estructura visual del Builder:

El diseño, layout, y organización de los componentes ya construidos se debe mantener exactamente igual.

Solo puedes mapear datos reales, agregar hooks, estados o handlers que los hagan funcionales.

Respetar la navegación actual:

No debes cambiar rutas, paths, ni jerarquías de navegación ya definidas.

Si necesitas agregar una nueva vista, se debe hacer sin afectar las existentes.

No usar mocks si ya existe el endpoint:

Si un componente aún usa datos mock, debes conectar el endpoint real del backend, respetando el formato y la estructura de datos que espera el componente.

Ajustes finos solo si son necesarios para integrarse:

Puedes cambiar nombres de campos, tipos de dato, nombres de props o estructura de objetos solo si eso permite que el componente consuma correctamente los datos del backend.

No sobreescribas componentes funcionales:

Si un componente ya tiene lógica de render, validación o consumo de datos (aunque incompleto), no lo sobrescribas por uno nuevo.

🔒 REGLAS DE SEGURIDAD Y COHERENCIA GLOBAL
Siempre analiza TODO el proyecto antes de ejecutar cambios:

No propongas cambios sin haber entendido la relación completa entre front y back.

No inventes nuevas estructuras si ya existe una funcional:

No cambies modelos de datos solo por conveniencia.

Mantén compatibilidad bidireccional:

Toda sugerencia en frontend debe estar basada en la estructura exacta del backend.

Toda lógica nueva en backend debe considerar lo que ya está hecho en frontend.

No rompas flujos funcionales por "simplificar":

Si algo ya funciona, no lo modifiques solo por reducir líneas o “optimizar”.

Respeta todas las funciones existentes:

Si un componente tiene onSubmit, onChange, onClick, o validaciones ya creadas, conéctalo a lo nuevo sin borrar lo anterior.

📌 EXTRAS (Sugerencias y protección)
Toda IA debe trabajar en función de integrarse al sistema actual, no de reinventarlo.

Los nuevos módulos/componentes deben inspirarse en los que ya existen: nombres, estructuras, lógica, para mantener la coherencia.

No repliques código sin verificar si ya existe: busca primero si ya hay un componente que hace lo que necesitas y solo extiéndelo.

Documenta cada cambio que propongas: indica qué archivo tocaste, por qué y cómo afecta a otros módulos.

Toda propuesta debe incluir pruebas funcionales: si propones una conexión, también debes indicar cómo probarla.
globs:
🎨 REGLAS PARA IA FRONTEND (Builder Exportado a React)
NO REEMPLAZAR COMPONENTES CREADOS CON BUILDER:

Los componentes existentes (vistas, formularios, botones, tabs, tablas) no deben eliminarse ni ser reemplazados por nuevos componentes genéricos o diferentes.

Solo puedes agregar lógica, props o funciones que permitan adaptarlos al backend real.

No alterar la estructura visual del Builder:

El diseño, layout, y organización de los componentes ya construidos se debe mantener exactamente igual.

Solo puedes mapear datos reales, agregar hooks, estados o handlers que los hagan funcionales.

Respetar la navegación actual:

No debes cambiar rutas, paths, ni jerarquías de navegación ya definidas.

Si necesitas agregar una nueva vista, se debe hacer sin afectar las existentes.

No usar mocks si ya existe el endpoint:

Si un componente aún usa datos mock, debes conectar el endpoint real del backend, respetando el formato y la estructura de datos que espera el componente.

Ajustes finos solo si son necesarios para integrarse:

Puedes cambiar nombres de campos, tipos de dato, nombres de props o estructura de objetos solo si eso permite que el componente consuma correctamente los datos del backend.

No sobreescribas componentes funcionales:

Si un componente ya tiene lógica de render, validación o consumo de datos (aunque incompleto), no lo sobrescribas por uno nuevo.

🔒 REGLAS DE SEGURIDAD Y COHERENCIA GLOBAL
Siempre analiza TODO el proyecto antes de ejecutar cambios:

No propongas cambios sin haber entendido la relación completa entre front y back.

No inventes nuevas estructuras si ya existe una funcional:

No cambies modelos de datos solo por conveniencia.

Mantén compatibilidad bidireccional:

Toda sugerencia en frontend debe estar basada en la estructura exacta del backend.

Toda lógica nueva en backend debe considerar lo que ya está hecho en frontend.

No rompas flujos funcionales por "simplificar":

Si algo ya funciona, no lo modifiques solo por reducir líneas o “optimizar”.

Respeta todas las funciones existentes:

Si un componente tiene onSubmit, onChange, onClick, o validaciones ya creadas, conéctalo a lo nuevo sin borrar lo anterior.

📌 EXTRAS (Sugerencias y protección)
Toda IA debe trabajar en función de integrarse al sistema actual, no de reinventarlo.

Los nuevos módulos/componentes deben inspirarse en los que ya existen: nombres, estructuras, lógica, para mantener la coherencia.

No repliques código sin verificar si ya existe: busca primero si ya hay un componente que hace lo que necesitas y solo extiéndelo.

Documenta cada cambio que propongas: indica qué archivo tocaste, por qué y cómo afecta a otros módulos.

Toda propuesta debe incluir pruebas funcionales: si propones una conexión, también debes indicar cómo probarla.
alwaysApply: true
---
